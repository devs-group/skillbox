apiVersion: v1
kind: Namespace
metadata:
  name: sandbox
  labels:
    # Baseline allows hostPath (needed for Docker socket proxy sidecar)
    # but still blocks privileged containers and host namespaces.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
