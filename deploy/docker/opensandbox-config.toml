[server]
host = "0.0.0.0"
port = 8080
api_key_required = true

[runtime]
type = "docker"
docker_socket = "/var/run/docker.sock"

[sandbox]
default_timeout = 600
max_timeout = 3600
default_resources.cpu = "500m"
default_resources.memory = "256Mi"
